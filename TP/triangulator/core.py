
#l'algorithme de triangulation principal
from typing import List, Tuple

Point = Tuple[float, float]
Triangle = Tuple[int, int, int]

def compute_triangulation(points: List[Point]) -> Tuple[List[Point], List[Triangle]]:

    print(f"La fonction compute_triangulation a été appelée avec {len(points)} points.")
    
    raise NotImplementedError("n'est pas implémenter")